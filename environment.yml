name: test_react

channels:
  - conda-forge

dependencies:
  # ---- Python runtime ----
  - python=3.11

  # ---- FastAPI backend ----
  - fastapi
  - uvicorn
  - pydantic

  # ---- Useful extras (very common with FastAPI) ----
  - python-dotenv
  - requests

  # ---- Node.js for React frontend ----
  - nodejs=20

  # ---- Optional but handy ----
  - git

